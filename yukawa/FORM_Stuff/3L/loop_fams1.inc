id k1.k1 * Sector(topoA) = +1*Den(1)*Sector(topoA)*DD1+Num(m2)*Den(1)*Sector(topoA)*1;
id k1.k2 * Sector(topoA) = +1*Den(2)*Sector(topoA)*DD1+1*Den(2)*Sector(topoA)*DD2+(-1)*Den(2)*Sector(topoA)*DD7+Num(m2)*Den(1)*Sector(topoA)*1;
id k1.k3 * Sector(topoA) = +1*Den(2)*Sector(topoA)*DD1+1*Den(2)*Sector(topoA)*DD3+(-1)*Den(2)*Sector(topoA)*DD8+Num(m2)*Den(1)*Sector(topoA)*1;
id k1.p1 * Sector(topoA) = +1*Den(2)*Sector(topoA)*DD1+(-1)*Den(2)*Sector(topoA)*DD4+Num(pp)*Den(2)*Sector(topoA)*1;
id k2.k2 * Sector(topoA) = +1*Den(1)*Sector(topoA)*DD2+Num(m2)*Den(1)*Sector(topoA)*1;
id k2.k3 * Sector(topoA) = +1*Den(2)*Sector(topoA)*DD2+1*Den(2)*Sector(topoA)*DD3+(-1)*Den(2)*Sector(topoA)*DD9+Num(m2)*Den(1)*Sector(topoA)*1;
id k2.p1 * Sector(topoA) = +1*Den(2)*Sector(topoA)*DD2+(-1)*Den(2)*Sector(topoA)*DD5+Num(pp)*Den(2)*Sector(topoA)*1;
id k3.k3 * Sector(topoA) = +1*Den(1)*Sector(topoA)*DD3+Num(m2)*Den(1)*Sector(topoA)*1;
id k3.p1 * Sector(topoA) = +1*Den(2)*Sector(topoA)*DD3+(-1)*Den(2)*Sector(topoA)*DD6+Num(pp)*Den(2)*Sector(topoA)*1;
